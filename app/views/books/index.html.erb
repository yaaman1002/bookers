 <p id="notice"><%= notice %></p>
 <h1>Books</h1>
    <table>
        <thread>
            <tr>
                <th>Title</th>
                <th>Body</th>
                <th colspan="3"></th>
            </tr>
        </thread>
        <tbody>
            <% @books.each do |book| %>
            <tr><td>
                  <span><%= book.title %></span></td>
                  <td><%= book.body %></td>
                <td><%=link_to 'Show', book_path(book.id) %></td>
                <td><%=link_to 'Edit',edit_book_path(book.id) %></td>
                <td><%=link_to 'Destroy',books_path, method: :delete, "data-confirm"=>"Are you sure?" %></td>
                </tr>
             <% end %>
        </tbody>　
     </table>
     
     <h2>New book</h2>
     <form class="new_book"id="new_book"action="/books"accept-charset="UTF-8"method="post">
       <input name="utf8"type="hidden"value="✓"><% if @book.errors.any? %>
       <input type="hidden"name="authenticity_token"value="6rL4nq74X4qHnSBiCRyqRjgHFo2ZHQr9yeNHGyCho5XiTGcY6V2rH7roDmnaEcGpEFeDPDOKoqNrQB6uEaF4bQ==">
       <div id="error_explanation">
         <h2>2 errors prohibited this book from being saved:</h2>
          <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul></div>
<% end %>
     <p><%= form_with model: @book do |f| %></p>
         <h4>title</h4>
             <div class="field">
               <div class="field with errors">
       <%= f.text_field :title %></div>
  <h4>body</h4>
   <div class="field with errors">
    <%= f.text_area :body %></div>
   <%= f.submit 'Create Book' %>
  <% end %>
   </div>
  </form>
</html>
